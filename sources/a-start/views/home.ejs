<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Home Page</title>
  <link href="https://unpkg.com/material-components-web@latest/dist/material-components-web.min.css" rel="stylesheet">
  <script src="https://unpkg.com/material-components-web@latest/dist/material-components-web.min.js"></script>
  <link rel="stylesheet" href="https://fonts.googleapis.com/icon?family=Material+Icons">
  <style>
    * {
      margin: 0;
      padding: 0;
    }

    .mdc-select,
    .mdc-text-field {
      margin-top: 8px;
      margin-bottom: 8px;
    }

    form {
      margin: 0em 1em 1em 1em;
    }

    form>div {
      margin-top: 8px;
      margin-bottom: 8px;
    }

    form>div>label {
      width: 30em;
    }

    form>div>button {
      width: 34.5em;
    }
  </style>
</head>

<body>
  <header class="mdc-top-app-bar">
    <div class="mdc-top-app-bar__row">
      <section class="mdc-top-app-bar__section mdc-top-app-bar__section--align-start">
        <span class="mdc-top-app-bar__title">Home</span>
      </section>
    </div>
  </header>

  <div class="mdc-top-app-bar--fixed-adjust">

    <div class="mdc-chip-set" role="grid" style="margin-top: 8px;">
      <div class="mdc-chip" role="row" style="margin-left: 10px;">
        <div class="mdc-chip__ripple"></div>
        <span role="gridcell">
          <span role="button" tabindex="0" class="mdc-chip__primary-action">
            <span class="mdc-chip__text">Hello World</span>
          </span>
        </span>
      </div>
    </div>

    <form method="GET" action="/logout">
      <div>
        <button class="mdc-button mdc-button--raised" style="height: 4em;">
          <div class="mdc-button__ripple"></div>
          <i class="material-icons mdc-button__icon" aria-hidden="true">info</i>
          <span class="mdc-button__label">Logout</span>
        </button>
      </div>
    </form>
  </div>


  <script language="javascript">
    const MDCTopAppBar = mdc.topAppBar.MDCTopAppBar;
    const MDCRipple = mdc.ripple.MDCRipple;
    const MDCTextField = mdc.textField.MDCTextField;

    const topAppBarElement = document.querySelector('.mdc-top-app-bar');
    const topAppBar = new MDCTopAppBar(topAppBarElement);

    const textFieldElement = document.querySelectorAll('.mdc-text-field');
    textFieldElement.forEach(e => new MDCTextField(e));

    const buttonElement = document.querySelectorAll('.mdc-button');
    buttonElement.forEach(e => new MDCRipple(e));
  </script>
</body>

</html>